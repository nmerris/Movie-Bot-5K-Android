<?xml version="1.0" encoding="utf-8"?>
<resources>

    <item name="activity_home_ref" type="layout">@layout/activity_single_fragment</item>

    <!-- NOTE: no need for a DetailPagerActivity ref because you can only get to it in phone mode -->
    <!-- ditto for FavoritesPagerActivity, they should just load the correct layout when they
    override SingleFragmentActivity.createFragment -->

</resources>